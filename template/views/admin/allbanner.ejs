<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/admin/articlewriting.css" />
  </head>
  <body>
    <div class="header">
      <p>Banners</p>
    </div>

    <div class="all-listed-articles">
      <div class="numbers">Created</div>
      <div class="title">Description</div>
      <div class="live-check">Check Out</div>
    </div>

    <% if(typeof bannerData != 'undefined'){ %> <% for (let index = 0; index <
    bannerData.length; index++) { %>

    <div class="main-content">
      <div class="no-of-articles">
        <p><%= bannerData[index].date %></p>
      </div>

      <div class="content-description">
        <% if(bannerData[index].description != " "){ %>

        <p><%= bannerData[index].description %></p>

        <% } else { %>

        <p>No description found</p>

        <% } %>
      </div>

      <div class="live-link">
        <a href="/admin/banner/<%= bannerData[index]._id%>/preview-banner"
          >Go</a
        >
      </div>
    </div>

    <div class="single-line-border"></div>

    <% } %> <% } %>

    <div class="button-container">
      <button>
        <a href="/admin/banner/create-banner"> >> Upload a New Banner </a>
      </button>
    </div>
  </body>
</html>
